import 'package:manga_dex_api/manga_dex_api.dart';

void main() async {

  final parameter = SearchMangaParameter();
  // final source = AsuraScanSource();
  // final logbox = LogBox();
  // final webview = HeadlessWebviewManager(log: logbox);
  // final cache = DefaultCacheManager();

  // final document = await webview.open(
  //   source.searchMangaUseCase.url(parameter: parameter),
  //   scripts: source.searchMangaUseCase.scripts,
  // );
  //
  // final mangas = await source.searchMangaUseCase.parse(
  //   root: document,
  //   cache: cache,
  // );
  //
  // final hasNext = await source.searchMangaUseCase.haveNextPage(
  //   root: document,
  //   cache: cache,
  // );
  //
  // for (final manga in mangas) {
  //   print('Manga: ${manga.title}');
  // }
  //
  // print('Has next: $hasNext');

  print('Hello World');
}
