name: mangastash_extension
description: A starting point for Dart libraries or applications.
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.7.2 <4.0.0"
  flutter: ">=3.29.3"

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
  melos: ^6.2.0
