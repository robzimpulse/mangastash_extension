name: mangastash_extension
description: A starting point for Dart libraries or applications.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.8.1

# Add regular dependencies here.
dependencies:
  dart_eval: ^0.8.2
  html: ^0.15.5
  flutter_cache_manager: ^3.3.1
  intl: ^0.19.0
  change_case: ^2.2.0
  collection: ^1.16.0
  flutter_inappwebview: ^6.1.0
  eval_annotation: ^0.8.1

  manga_dex_api:
    git:
      url: https://github.com/robzimpulse/mangastash
      ref: dependency_adjustment
      path: module/library/manga_dex_api
  entity_manga:
    git:
      url: https://github.com/robzimpulse/mangastash
      ref: dependency_adjustment
      path: module/entity/entity_manga

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
